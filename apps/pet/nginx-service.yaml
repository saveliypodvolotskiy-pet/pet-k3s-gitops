apiVersion: v1
kind: Service
metadata:
  name: selfmadeservice-nginx
  namespace: pet
spec:
  ports:
    - name: my-app
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: mainApp
  type: ClusterIP
